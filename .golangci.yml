# More info on config here: https://github.com/golangci/golangci-lint#config-file
linters:
  disable-all: true
  enable:
    - gci
    - errcheck
    - gosimple
    - ineffassign
    - unused

linters-settings:
  gci:
    sections:
      - standard
      - default
      - prefix(github.com/yasonofriychuk/service-codewars)
    skip-generated: true
